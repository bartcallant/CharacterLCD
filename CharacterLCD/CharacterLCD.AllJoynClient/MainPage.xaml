<Page
    x:Class="CharacterLCD.AllJoynClient.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CharacterLCD.AllJoynClient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,40">
                <TextBlock Text="Status:" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding Status}" />
            </StackPanel>
            <TextBox Margin="0,0,0,10" Text="{Binding Message, Mode=TwoWay}" Width="250" />
            <Button Content="Send" Command="{Binding SendMessage}" Width="100" Margin="75,0,0,0" IsEnabled="{Binding IsConnected}" />
        </StackPanel>
    </ScrollViewer>
</Page>
